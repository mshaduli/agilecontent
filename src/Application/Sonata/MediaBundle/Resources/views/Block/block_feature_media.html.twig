{#

This file is part of the Sonata package.

(c) Thomas Rabaix <thomas.rabaix@sonata-project.org>

For the full copyright and license information, please view the LICENSE
file that was distributed with this source code.

#}
{% extends 'SonataBlockBundle:Block:block_base.html.twig' %}

{% block block %}

    <div class="sonata-media-block-feature-media-container">

        <div class="sonata-media-block-feature-media-container-{{ settings.orientation }}">
            {% if media %}
                {% media media, settings.format %}
            {% else %}
                {# no media selected ... #}
            {% endif %}
        </div>

        {% if settings.title %}
            <h3 class="sonata-media-block-feature-media-title">{{ settings.title }}</h3>
        {% endif %}

        <div class="sonata-media-block-feature-media-content">
            {{ settings.content }}
        </div>

        <div style="clear: both"></div>
    </div>
    {% if sonata_page.isEditor or app.security and app.security.isGranted('ROLE_PREVIOUS_ADMIN') %}
    <a class="btn-primary btn" id="clickBtn" href="http://rutherglen.local/app_dev.php/admin/sonata/media/media/list" data-toggle="modal" data-target="#myModal_{{ block.id }}">Edit</a>
    <div class="modal fade" id="myModal_{{ block.id }}" style="width:80%;height:80%;overflow: scroll; left: 30%"></div>
    {% endif %}
{% endblock %}